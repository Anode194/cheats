% network linux

# curses network manager
nmtui

# network manager cli
$ nmcli

% tmux, terminal emulator

# create a verticle split
#
# prefix %

% macOS, brew, 

# search for packages using fzf
# 
brew search --desc $(brew search | fzf)
